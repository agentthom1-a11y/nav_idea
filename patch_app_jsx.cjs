const fs = require('fs');
// Let's replace createBrowserRouter with HashRouter or BrowserRouter just in case 
// there's a weird router init bug. 
// Actually, no, createBrowserRouter is fine. Let's just wait for install.
